server:
  port: 8080
  shutdown_timeout: 10s

postgres:
  url: "postgres://user:password@localhost:5432/cron?sslmode=disable"

rabbitmq:
  url: "amqp://guest:guest@localhost:5672/"
  queue_name: "cron_jobs"

scheduler:
  concurrency: 10
  webhook_timeout: 3s
  job_ttl: 1h

prometheus:
  port: 9090
